# vertx-kuickstart-core

Core libraries used by microservices created from the vertx-kuickstart template.

Note that these libraries are currently in an alpha state and the interfaces are subject to change.